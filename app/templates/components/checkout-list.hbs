{{#each items as |item|}}
  {{ display-cart item=item }}
{{/each}}

<div class="row">
  <div class="col-xs-3 col-xs-offset-9">
    {{#if totalPrice}}
      <h3>Total:</h3>
      ${{totalPrice}}
    {{/if}}
  </div>
</div>
